<div class="tarjeta">
  {{#each imagenes}}
    <div class="card">
      <img src={{this.path}} alt={{this.title}} />
      <h3>{{this.title}}</h3>
      <h5>{{this.description}}</h5>
      <a class="btn" href="/image/{{this._id}}/delete">Eliminar</a>
    </div>
  {{/each}}
</div>